# 亿小步教育小程序 - UI 设计与图片尺寸规范

为了保证小程序在不同机型上的展示效果一致且美观，请在后续的 UI 设计和图片物料制作中，统一遵循以下尺寸和比例规范。

## 1. 活动封面图 (首页卡片)
首页的活动列表卡片所使用的封面大图。
- **标准尺寸**: `800 x 571` px
- **长宽比**: 约 `1.4 : 1`
- **说明**: 建议主要视觉元素居中存放，避免被卡片边缘遮挡。例如暑期营和寒假营的卡片封面都需要裁剪或设计为这个尺寸。

## 2. 活动详情页 - 顶部缩略图 (Banner)
点击进入活动详情后，位于页面最上方的大横幅图片。
- **标准尺寸**: `800 x 470` px
- **长宽比**: 约 `1.7 : 1`
- **说明**: 用于展示活动的核心主题，高度相比首页卡片略窄，留出更多屏幕空间给下方的正文内容。

## 3. 活动详情页 - 内容长图
详情页内的介绍图文（如活动介绍、课程收获、日程安排、导师团队等）。
- **标准宽度**: `800` px
- **标准高度**: **不限（Auto）**，根据实际内容长度自适应
- **排版建议**: 
  - 保持所有详情介绍图的宽度严格一致（800px），避免拼接时出现左右边缘不齐的情况。
  - 重要文字和元素的字号在 800px 宽度基准下需要清晰易读（在手机端通常会缩放至设备宽度）。

## 4. 图标与状态标签 (规范)
- 底部 TabBar 图标（如：活动 / 我的）：建议使用 `48 x 48` px 至 `80 x 80` px 等比例切图，保持清晰度。
- 状态标签（如“报名中”、“已结束”）：采用极简硬朗风格。背景采用白色半透明（如 `rgba(255, 255, 255, 0.95)`），**极小圆角（约 2rpx，视觉上近乎直角）**，收紧内边距，使标签极为紧凑精致。字号统一为 11px。

## 5. 全局排版与色彩规范 (Aesthetics)
- **页面背景与卡片**: 页面底层背景统一采用**纯白 `#ffffff`**。首页活动卡片等内容容器**不再使用大圆角，一律采用直角 `0px`** 设计，且去掉内外冗余留白，让大图拥有更强的视觉张力和高级感。
- **页面横向边距**: 全局统一的内容区域左右安全留白（Padding）缩紧为 **`16px`**。
- **字体层级与色彩**: 
  - 强调文本及选中态（如主要标题、Active 分类）：使用带有呼吸感的极深灰黑 `#111827`，适当加大字重（Weight 600/500）。
  - 普通段落文本或默认状态：使用更柔和的深灰 `#4b5563` 或 `#6b7280`。
  - 字号跨度拉开层级（例如主标题使用 `16px`，次级信息迅速回落至 `11.5px` 或 `12px`）。
- **功能控件底色**: 搜索框等基础控件采用极为通透的极浅灰 `#f9fafb` 背景色，以极其微弱的对比度在纯白背景里划分出控件区域。搜索框保留完全的胶囊形圆角（`999px`）。

---

> **给设计师的建议**
> 所有的设计稿可以按照 `800px` 为屏幕宽度基准线进行出图，这样能保证素材原图在常见高清手机屏幕（如 390px、414px 或 430px 的 @2x / @3x 屏幕）上都拥有极佳的清晰度和一致性。导出图片时建议压缩至合理大小（每张不要超过 500KB）以加快小程序的加载速度。
