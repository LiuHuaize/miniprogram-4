page {
  height: 100vh;
  background: linear-gradient(180deg, #f8fafc 0%, #eef3f8 100%);
  color: #0f172a;
  font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
}

.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
}

.status-spacer {
  height: 44px;
}

.nav-bar {
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-title {
  font-size: 16px;
  font-weight: 600;
  color: #0f172a;
}

.profile-card {
  margin: 8px 16px 16px;
  padding: 16px;
  border-radius: 18px;
  background: linear-gradient(180deg, #ffffff 0%, #f9fbff 100%);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  border: 1px solid rgba(148, 163, 184, 0.2);
  box-shadow: 0 12px 28px rgba(15, 23, 42, 0.1);
}

.profile-left {
  display: flex;
  flex-direction: column;
  gap: 6px;
  flex: 1;
  min-width: 0;
}

.profile-greeting {
  font-size: 16px;
  font-weight: 600;
  color: #0f172a;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.profile-sub {
  font-size: 12px;
  color: #94a3b8;
}

.profile-avatar {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: #f3f6f9;
  border: 1px solid rgba(148, 163, 184, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  flex-shrink: 0;
}

.profile-avatar-image {
  width: 100%;
  height: 100%;
  display: block;
}

.menu-section {
  padding: 0 16px 120px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  box-sizing: border-box;
}

.menu-card {
  background: #ffffff;
  border-radius: 16px;
  border: 1px solid rgba(148, 163, 184, 0.2);
  overflow: hidden;
}

.menu-item {
  height: 52px;
  display: flex;
  align-items: center;
  padding: 0 16px;
  gap: 12px;
}

.menu-item + .menu-item {
  border-top: 1px solid rgba(148, 163, 184, 0.12);
}

.menu-item--center {
  justify-content: center;
  gap: 8px;
  color: #6b7280;
}

.menu-icon {
  width: 32px;
  height: 32px;
  border-radius: 10px;
  background: #f2f6f9;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #64748b;
}

.menu-icon--plain {
  width: 24px;
  height: 24px;
  border-radius: 12px;
  background: transparent;
}

.menu-text {
  font-size: 14px;
  color: #111827;
}

.bottom-bar {
  --td-tab-bar-active-color: #2f7d75;
  --td-tab-bar-color: #94a3b8;
  --td-tab-bar-border-color: #e2e8f0;
  --td-tab-bar-height: 96rpx;
  --td-font-body-extraSmall: 22rpx;
  background-color: rgba(255, 255, 255, 0.94);
  backdrop-filter: blur(10px);
}

.tab-icon {
  display: flex;
  align-items: center;
  justify-content: center;
}

.home-indicator {
  position: fixed;
  bottom: 8px;
  left: 50%;
  transform: translateX(-50%);
  width: 120px;
  height: 4px;
  border-radius: 999px;
  background: #0f172a;
  opacity: 0.12;
  pointer-events: none;
  z-index: 6;
}

.profile-editor-mask {
  position: fixed;
  inset: 0;
  background: rgba(15, 23, 42, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 20;
}

.profile-editor {
  width: 78%;
  max-width: 320px;
  background: #ffffff;
  border-radius: 18px;
  padding: 20px;
  box-sizing: border-box;
  box-shadow: 0 18px 40px rgba(15, 23, 42, 0.18);
}

.profile-editor-title {
  font-size: 16px;
  font-weight: 600;
  color: #0f172a;
  text-align: center;
  margin-bottom: 16px;
}

.profile-editor-body {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
}

.profile-editor-avatar {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0;
  background: transparent;
  border: none;
  box-sizing: border-box;
}

.profile-editor-avatar::after {
  border: none;
}

.profile-editor-avatar-ring {
  width: 92px;
  height: 92px;
  border-radius: 999px;
  padding: 3px;
  background: linear-gradient(135deg, #cbd5f5 0%, #a7f3d0 100%);
  box-shadow: 0 10px 24px rgba(15, 23, 42, 0.15);
  box-sizing: border-box;
}

.profile-editor-avatar-inner {
  width: 100%;
  height: 100%;
  border-radius: 999px;
  background: #f8fafc;
  border: 1px solid rgba(148, 163, 184, 0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.profile-editor-avatar-image {
  width: 100%;
  height: 100%;
  display: block;
}

.profile-editor-avatar-icon {
  color: #94a3b8;
  display: flex;
  align-items: center;
  justify-content: center;
}

.profile-editor-avatar-text {
  margin-top: 8px;
  font-size: 11px;
  color: #94a3b8;
  letter-spacing: 0.2px;
}

.profile-editor-input {
  width: 100%;
  height: 40px;
  padding: 0 12px;
  border-radius: 10px;
  border: 1px solid rgba(148, 163, 184, 0.4);
  font-size: 14px;
  color: #0f172a;
  box-sizing: border-box;
  background: #ffffff;
}

.profile-editor-actions {
  margin-top: 16px;
  display: flex;
  gap: 12px;
}

.profile-editor-button {
  flex: 1;
  height: 40px;
  border-radius: 999px;
  font-size: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.profile-editor-button::after {
  border: none;
}

.profile-editor-button--ghost {
  background: #f1f5f9;
  color: #475569;
}

.profile-editor-button--primary {
  background: #2f7d75;
  color: #ffffff;
}
